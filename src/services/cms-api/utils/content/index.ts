import { formatContent, removeUnwantedContent } from './format-content';
import { getMetaDescriptionFromContent } from './get-meta-description-from-content';

export const contentUtils = {
  getMetaDescriptionFromContent,
  removeUnwantedContent,
  formatContent,
};
